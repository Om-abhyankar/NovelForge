{
  "name": "novelforge-web-tests",
  "private": true,
  "description": "Not part of the deployed site - a Node-side regression test for the hand-written .docx generator/parser in app.js. Run: npm install && npm test",
  "scripts": {
    "test": "node test_docx.js"
  },
  "devDependencies": {
    "@xmldom/xmldom": "^0.9.0"
  }
}
